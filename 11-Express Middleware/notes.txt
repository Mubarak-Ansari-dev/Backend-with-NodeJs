Middleware is just a function who runs/executes on every res & res

//What is Middkeware??
Middleware functions are functions that have access to the request object(req), 
the response object(res), and thebnext Middleware function in the application;s request-response cycle.
The next middleware function is commonly denoted by a variable named next.

// Middkeware functions can perform thr following tasks;
   1- Execute any code
   2- Make changes to the request and the response objects
   3- End the request-response cycle
   4- Call the next middleware function in the stack.